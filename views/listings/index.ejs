<% layout("/layouts/boilerplate") -%>

<div class="container mt-3">
  <div class="row row-cols-lg-3 row-cols-md-2 row-cols-1 g-4">
    <% for(let listing of allListings) { %>
    <div
      class="card col listing-card"
      style="width: 18rem; display: flex; flex-direction: column; height: 100%;"
    >
      <a href="/listings/<%= listing._id %>" class="listing-link">
        <div class="card-body" style="flex-grow: 1">
          <h5 class="card-title"><%= listing.title %></h5>
          <h6 class="card-subtitle mb-2 text-body-secondary">
            Date: <%= moment(listing.date).tz("Asia/Kolkata").format("DD MMMM YYYY") %>
          </h6>
          <p class="card-text"><%= listing.content %></p>
        </div>
        <div class="card-footer text-muted text-center">
          <%= moment(listing.date).tz("Asia/Kolkata").format("hh:mm A") %>
        </div>
      </a>
    </div>
    <% } %>
  </div>
</div>
